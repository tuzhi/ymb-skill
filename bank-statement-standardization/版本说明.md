# 版本说明 / CHANGELOG

本技能（银行流水标准化 bank-statement-standardization）相对最早版本的更新记录。

---

## 控制契约收敛（2026-08-12）

- 密码重试以 `StandardizationRequest.file_passwords` 作为正式契约并全链路内存传递；兼容客户目录历史 `_file_hints.yaml`，由 Runtime 建立快照后一次性转换为同一内存映射并立即删除。Core 不再隐式查找提示文件，密码不进入 Run 快照、结果、日志或错误包，Stage 1 结束后立即清空内存映射。
- `StatementService` 收敛为真正的同步调用：直接执行 Runner 并消费 `PipelineExecutionResult`，删除内部线程池、活动 Run 状态、`RunReference`/`RunDetail` 和 Child Run 完成后的文件回读。
- `ZERO_TRANSACTION_STATEMENT` 纳入统一 `ReasonCode`，确定性返回 `REQUEST_USER`，不再错误进入 AI Repair。
- 删除 `PipelineExecutionResult` 中已被 `stages`/`deliverable` 覆盖的 `stage_summaries` 与 `artifacts` 重复字段；Harness 的 `to_summary_dict()` 结构保持不变。
- `NextAction`、`ReasonCode`、`RunDecision` 和 `RunResult` 统一归入 `runtime.models.run_result`；入口提示与失败路由不再通过位置参数和散落字符串拼装。
- 重试上限收敛为 `RetryPolicy`；删除 `MAX_PASSWORD_ATTEMPTS`、`MAX_AI_REPAIR_ATTEMPTS` 和重复原因常量。
- `RunResult.from_pipeline_result()` 取代 `RUN_RESULT_FIELDS` 与独立转换函数；只接受当前扁平 `pipeline_result.json`，删除旧 manifest/run_result/run_manifest 兼容读取。
- `RepairRequest`、`RepairResult`、`RepairStatus` 统一 Repair 跨会话契约；删除 `contracts.py`、`UNSUPPORTED` 和 `CHILD_RUN_READY`，修复提交成功后直接启动 Child Run 验收。
- 用户原始输入改为 PDF/Excel 严格白名单；删除 `KNOWN_NONSTATEMENT_EXT` 与 `skipped_inputs` 业务旁路，不支持格式和重复文件明确返回 `INPUT_SOURCE_INVALID`。
- WPS PDF 转 Excel 来源元数据前移到输入真实性门禁；命中后直接返回 `INPUT_SOURCE_INVALID / REQUEST_USER`，不进入 Reader 或 AI Repair。检测规则独立封装，便于后续迁移到反欺诈模块。
- `pipeline_result.json` 成为唯一结构化结果，直接内联 `file_results` 与 `qc`；删除新 Run 对 `stage_1_results.json`/`qc_results.json` 的生成和 Validator、Repair Coordinator、父子 Run 及 QA 工具的读取依赖。
- 默认 Run 不再生成 `events.jsonl`、`receipts/`、`traceback.txt` 和 ERROR/WARNING ZIP；`--verbose` 显式开启事件/回执，`--error-bundle-mode safe|full` 显式生成诊断包。`token_usage.json` 延迟到 Repair 宿主真正提交用量时才创建。

## v1.4.11（2026-08-07）

- 将 `manifest.json` 与 `run_result.json` 合并为 `pipeline_result.json`：整体结果统一保存 Run 元数据、Stage 状态与摘要、产物索引和最终 RunResult；`stage_1_results.json` 与 `qc_results.json` 继续作为独立事实文件。父子 Run、执行计划和 SDK 只读取 `pipeline_result.json`，不再保留旧结果文件兼容分支。
- 精简 `pipeline_result.json`：最终决策展开到顶层，`stage_summaries` 合入 `stages.<stage_id>.summary`，最终文件改用字符串数组 `deliverables`；移除重复的 `run_result`、`exit_code`、中间 `artifacts` 以及 `token_usage_ref`。`token_usage.json` 继续作为独立 Harness 统计文件。
- 根目录 `pyproject.toml` 与 Pipeline Result 模板版本同步为 1.4.11；继续发布 macOS、Windows 两个 Skill 包，WorkBuddy 专家包独立保持 1.0.3。

## v1.4.10（2026-08-07）

- 新增 `YamlRuleService`、`StatementService` 和 `BiAnalysisService` 的明确服务边界；标准化与草稿测试统一使用调用方传入的不可变规则快照。
- Pipeline 以 `PipelineExecutionResult` 在内存中交接 Stage、QC、产物和 RunResult；Service 不再为了组装结果回读 receipts。
- DTO 统一收敛到各模块 `models/`，标准化输入固定为 `InputFile(file_name, file_path, file_md5)`，删除上传 stream 和旧字段兼容层。
- `orchestrator.py` 拆分为薄 CLI，Runner、执行计划、输入快照、失败策略和结果存储分属 `runtime/` 模块；删除旧 CLI 对内部符号的兼容重导出。
- 根目录 `pyproject.toml` 与运行模板版本同步为 1.4.10；继续发布 macOS、Windows 两个 Skill 包，WorkBuddy 专家包独立保持 1.0.3。

## v1.4.9（2026-08-06）

- 默认 Run 根目录恢复为当前工作目录下的 `./runs`，避免调用方遗漏 `--run-root` 时把交付物写入 Skill 安装目录。
- Repair Token 用量增加 `not_started / available / partial / unavailable` 测量状态；宿主未回传时使用 `null`，不再把未知消耗误记为 0。
- Token 规范化和状态判断收敛为一次计算，receipt 与 `token_usage.json` 直接复用；删除同一 Run 不会发生的多 Repair session 聚合逻辑。
- 发布模板变量统一为 `{{PLATFORM_COMMAND}}`，继续生成 macOS、Windows 两个平台包；WorkBuddy 专家包沿用独立版本 1.0.3。
- 根目录 `pyproject.toml` 与 Manifest 模板版本同步为 1.4.9。

## Harness 交接精简（2026-08-05）

- 取消 Fallback、Audit、evidence bundle、Policy Gate 和角色 task 中间层，Harness 收敛为“确定性 Coordinator + 一个隔离 Repair Agent”。
- Stage 1 失败事实保留在 `pipeline_result.json.file_results`。Orchestrator 直接返回 `NEED_REPAIR + request`，专家不需要先调用 Coordinator `next`。
- Repair request 从 `pipeline_result.json.file_results` 获取失败文件，只引用失败原 PDF/Excel、`roles/repair.md` 和输出契约；Agent 直接读原文件，不再经由脱敏证据摘要，也不维护 routing YAML。
- Repair Agent 可自由选择解析和修复方法，只需为当前失败文件写入 `repair/attempt-N/standardized/*__standardized.csv` 并返回 `repair_result`。
- Coordinator `submit` 校验 request、真实新 `session_id`、源文件 MD5、结果 checksum 和路径范围，随后自动创建 Child Run。Child Run 复用父 Run 已成功文件并把 Repair CSV 交给原 Validator、QC 和后续 Pipeline。
- Child Run 再次需要修复时直接返回新的 `NEED_REPAIR + request`；Coordinator CLI 删除 `next` 操作，专家不再手工推进或修改 manifest。
- Coordinator 每次 `NEED_REPAIR` 和 `SUBMIT` 决策以紧凑 stderr 事件写入 WorkBuddy 可见日志；修复提交成功后直接启动 Child Run，stdout 最后一行仍只保留机器可读协议。

## parser-iteration-2026-08-05-v1（Fallback PDF 表头证据回填）

- 审计范围：WorkBuddy workspace `/Users/tuzhi/Workbuddy/2026-08-05-10-29-18`，目标 Run `20260805T102924+0800-b84a7745`；确认 Fallback 隔离会话 `agent-02d70b4a` 已真实启动，但收到的 `routing_evidence.header_candidates` 为空。
- 问题分类：交通银行模板缺规则用于验证 `parser-gap` 的 AI Fallback 路线；通用 PDF Reader 已恢复表格但未回填表头属于 `generic-capability / evidence-contract gap`。原 PDF 可解析，不是文件损坏或密码问题。
- 正式修复仅补齐证据契约：unmatched PDF 的通用 Reader 成功后，由程序回填 `reader_id` 和交易正文前的表头候选。最多 3 行、每行 30 列，沿用证据脱敏，不给 Fallback 增加原 PDF 读取权限。
- 受控复现：回放时主动排除新增交通银行规则，仍得到 `decision=unmatched`、`reader_id=pdfplumber_table` 和完整 11 列双语表头；证据不含长账号或交易金额。
- 诊断结果：通用 Reader 可恢复交通银行 2,930 笔记录，但生产严格 YAML 门禁仍按预期阻断；该样本继续保持 unmatched，用于 WorkBuddy 专家 Fallback 新会话验证。
- 变更落点：`readers/routing/evidence.py`、`readers/pdf_input.py` 及 Router 证据回归测试。故意不发布交通银行 YAML fingerprint，也未采纳“Fallback 直接读取原 PDF”的权限扩张。

## v1.4.8（2026-08-05）

- 新增 POSIX/Windows 有限候选启动器，支持 `YMB_WORKBUDDY_PYTHON`、WorkBuddy 常见运行时位置和 PATH；不递归扫描安装目录。
- 平台 launcher 直接执行 `orchestrator.py` 或 `fallback_coordinator.py`，删除 `skill_entry.py`、`subprocess` 包装和 stdout 二次转发。
- 输入校验、活动 Run 幂等计划和合法 RunResult 的零退出码归入 Orchestrator；`AI_FALLBACK`、`REQUEST_USER`、`REPORT_ERROR` 是业务路由，不再被 Shell 误判为程序失败。
- Skill 删除硬编码 macOS 内联入口和 Agent 调度协议；从同一份源码分别裁剪 macOS、Windows 两个平台 zip，每个包只暴露本平台 launcher，独立 Skill 遇到 `AI_FALLBACK` 只交给上层。
- `SKILL.md` 使用单一 `{{PLATFORM_INLINE_ENTRY}}` 发布变量；打包器分别渲染为 macOS/Windows 的 `!` 内联命令，使 Skill 触发后立即执行 launcher，恢复成功快速路径，不把正常执行命令迁入专家 Prompt。
- 新增可版本化的 WorkBuddy 流水标准化专家包；v1.0.3 改用 `.workbuddy-plugin`，补齐三个 Agent 的展示字段、3 个标签、3 个推荐问题、49 字中文能力介绍和 512×512 头像。主专家按 `NEED_FALLBACK` / `NEED_AUDIT` 选择专用 `subagent_type`，子 Agent 仍按 task 引用读取 Skill 内业务 Prompt和契约，不复制业务规则。
- 角色 task 新增确定性的 `run_dir`，使隔离 Agent 能把紧凑相对 `input_refs` 无歧义解析到父 Run，不需要扫描工作区。
- Fallback task 的 `input_refs` 只暴露脱敏的 `evidence_bundle.json`；`fallback_request.json` 保留为 Coordinator 控制事实，不再进入 AI 会话。重试时仍仅追加上一轮角色结果、拒绝证据和 Policy Gate 摘要。
- 根目录 `pyproject.toml` 与 Manifest 模板版本同步为 1.4.8。

## v1.4.7（2026-08-04）

- WorkBuddy 内联入口改为快速生成 `EXECUTE_PIPELINE` 计划，不再在 Skill 加载命令中等待完整 Pipeline。
- 执行计划携带一次性 Orchestrator 命令和 10 分钟宿主超时；超时或空返回时禁止扫描 Run、读取源码或重复启动。
- 同一输入的未完成计划复用预分配 `run_id`；Orchestrator 原子认领 Run，误重复执行时附着原结果，不产生第二个 Run。

## v1.4.6（2026-08-04）

- 保持一套 Skill 源码，发布时只渲染平台 Python 入口，分别生成 macOS 与 Windows 两个 zip。
- macOS 使用 WorkBuddy `default/bin/python`；Windows 使用日志实测可用的 `default/Scripts/python.exe`。两端均由 Bash 展开 `${HOME}` 和 `${CODEBUDDY_SKILL_DIR}`，不硬编码用户名。
- 不增加 installer、Hook、PyInstaller、私有运行时或用户 PATH 修改；正常路径继续由薄 Skill 直接执行 `skill_entry.py`。

## v1.4.5（2026-08-04）

- WorkBuddy 内联入口直接使用其 Shell `PATH` 中的 `python`，不再硬编码 macOS 或 Windows 的内部虚拟环境目录。
- `DELIVER` 成功路径只交付一次文件并使用 `summary` 返回，停止额外读取 manifest、QC、报告、目录或写入 memory。

## v1.4.4（2026-08-02）

- Policy Gate 或 Fallback 契约拒绝后由 Coordinator 签发不可变 `retry-decisions/attempt-XX.json`；最多进入 `attempt-02`，旧 attempt 不删除、不覆盖，第二次失败固定停止。
- Fallback 输出模板声明受支持的 routing fingerprint DSL，明确 `metadata.all`、`style.all` 和账户类型枚举，避免入口会话读取源码后指导重试。
- Audit 的 `affected_file_ids` 必须与 Fallback 修复范围完全一致，否则不得创建 Child Run。
- 角色提交支持幂等恢复：输出或拒绝证据已落盘而 receipt 尚未完成时，可用相同 session 和结果补齐；Token 用量按 session 去重。
- 兼容恢复 v1.4.3 已接受且 checksum 有效的旧 receipt，并将 Manifest 与分发版本统一升级为 1.4.4。

## v1.4.3（2026-08-02）

- 薄 Skill 在 Stage 1 进入 AI Fallback 后，强制通过 WorkBuddy `Agent` 为 Fallback 与 Audit 分别创建独立 `general-purpose` 子会话，禁止 `fork`、`resume` 和入口会话代演。
- 角色结果只以紧凑 JSON 回到入口会话，并使用宿主返回的 `subAgent.sessionId` 提交 Coordinator；元数据缺失时停止，禁止模型自行生成 session ID。
- 保持单 Skill 安装和确定性 Coordinator，正常成功路径不创建 Agent 子会话。
- Manifest 模板版本与分发版本统一为 1.4.3。

## v1.4.2（2026-08-02）

- WorkBuddy 加载 Skill 时通过 `!` 内联命令直接启动确定性 Orchestrator；正常路径不再依赖 AI 决定何时执行程序。
- 新增仅使用标准库的 `skill_entry.py`：校验 `args` 输入路径并使用当前 WorkBuddy Python 执行原入口；缺少或无效输入只返回紧凑 `REQUEST_USER`，不扫描源码或依赖。
- 保持一个可安装 Skill、一个 Coordinator 和两个按需 AI 角色；成功路径仍不创建 Fallback/Audit 会话。

## v1.4.1（2026-08-01）

- 将 `PyYAML>=6,<7` 加入工作区和 Skill 分发包的正式运行依赖，避免全新环境首次执行因缺少 YAML 运行库而失败。
- 成功 `DELIVER` 的 RunResult 新增紧凑 `summary`，直接携带输入/处理文件数、QC 状态、告警数量和至多 5 条去重告警摘要，宿主无需回读 manifest/QC，并以程序事实避免文件数量误报。

## v1.4.0（2026-08-01）

- RunResult 新增按需 `action`：成功交付不输出空 action；密码请求和 Stage 1 AI Fallback 返回可执行的 handler、operation、run_dir 与必要引用，使入口 Skill 无需保存后续操作知识。
- `fallback_coordinator.py submit` 在保存不可变回执后自动推进；Audit 接受或密码重试后直接返回新 RunResult，routing 修复的 Child Run 幂等创建，不再要求入口 Skill 手工执行 `next`、`run-child` 或解包另一层结果。
- Harness 统一作为一个 `bank-statement-standardization_v1.4.0.zip` 发布；用户只安装一个 Skill，Coordinator 和两个角色协议随包提供。
- 将 Harness 收敛为一个确定性 Coordinator 和两个按需 AI 角色：入口 Skill 只执行 Orchestrator 和解释 RunResult；Fallback 与 Audit 位于 `roles/`，仅在 Coordinator 生成新会话 task 时读取。
- 新增 `run_result.json`，稳定输出 `DELIVER / REQUEST_USER / AI_FALLBACK / REPORT_ERROR`；CLI 默认只在 stdout 输出紧凑结果，完整事件和回执留在 Run 目录。
- Stage 1 新增结构化 reason code。密码、密码错误、无效源文件和硬 QC 直接请求用户或停止，不创建 AI fallback；Stage 2～4继续保持零 AI。
- `password_attempt` 与 `ai_repair_attempt` 分开计数；用户密码由 `StatementService` 在 Child Run 输入快照内确定性写入 `_file_hints.yaml`，不修改父 Run，也不占用 AI 修复次数。
- 新增 `harness/` 和 `fallback_coordinator.py`：程序生成紧凑 evidence、校验角色 JSON、强制 Fallback/Audit 使用不同 session_id、记录 token usage，并由 checksum 串联不可变 task、receipt 和结果。
- Policy Gate 仅验证身份、范围、routing 规则结构、当前 Run 目标命中和未受影响文件回归；语义修复必须经独立 Audit。通过后生成 Run 内 routing snapshot，Child Run 按 checksum 加载，绝不激活或发布正式 YAML。
- Fallback/Audit task 使用包内 `role_prompt_ref`，不再要求宿主额外安装或发现两个独立 Skill。
- 收敛 Review 中的无效协议：Audit 改读 evidence、单条修复和 Gate 摘要，不再加载完整 routing snapshot；Fallback request 不再重复保存详细错误与 route。
- 新增 `retry-password --password-stdin` 确定性重试入口；密码不进入命令行或 AI Fallback。`token_usage.json` 明确只统计 Fallback/Audit 宿主回传用量。
- 生产 Skill zip 排除 README、版本/测试报告和旧 `references/`；单包包含薄入口、两个按需角色协议和全部确定性运行组件。
- 修复 testdata metadata 审计对 `_open_pdf` 的旧模块导入，统一从 `readers.pdf_input` 使用实际实现。

## v1.3.1（2026-07-31）

- 收紧兴业银行个人交易明细 PDF 指纹：原 `md5:8626b51d2fac7f18ec01c5042e1b2731` 的 9 个真实表头由 `optional` 全部迁入 `required`，并新增 `match: reader_header` 的 Reader 后二段门禁，避免水印污染下仅凭局部文本即放行；新 fingerprint 为 `md5:a45eacad2710cb4976c2606901bc45c0`。
- 生产 Stage 1 默认开启严格 YAML 门禁：原始 PDF/Excel 只有唯一命中已发布 YAML 才能生成正式标准化产物；未命中或多命中以 `BLOCKED` 结束。阶段验收会拒绝历史 `unmatched + DONE` 结果，增量 Run 也不会复用此类旧产物；上游明确声明的 `__standardized.csv` 保持兼容。
- 调整严格 YAML 门禁顺序：先排除不具备流水结构的报告、名册和管线衍生产物，再阻断具备真实流水结构但未唯一命中 YAML 的原始文件；恢复吉安超创电子 PCB、英奇制衣客户交付，同时不放宽生产门禁。
- 修正浙江网商银行 PDF/Excel 的“借方金额（收）/贷方金额（支）”方向映射，并更新对应 fingerprint；三组 PDF/XLSX 收支笔数重新一致，逐笔余额连续性断点归零。
- 优化 `pdfplumber_coordinate_table` 落列：记录完整表头文字框，优先按正文起点是否唯一落入表头范围归列；窄缝取表头文字框间隙中点、表头重叠取中心点中点、宽空隙回退相邻表头起点中点，修复 `0.01～9.62` 等短金额被右对齐到相邻列的问题。
- 新增支付宝“余额收支流水证明”PDF 指纹，读取账户、户名、交易时间、对手、收支余额与账户余额；Router 顶层账户类型保持 `未知`，仅在抬头出现公司、合作社、个体工商户等明确证据时动态判断为对公。
- `stage_1_results.json` 的 `DONE` 文件新增保守识别类型与标准交易笔数，直接执行和父 Run 复用保持同一字段契约，供任务详情稳定展示。
- YAML 草稿测试统一覆盖指定 Run 的全部可测试输入，不再提供局部 `file_ids` 筛选，避免选择性测试后误发布规则。
- 运行时 Manifest 的每个阶段新增单一 `duration_seconds` 字段：新 Run 初始化为 `null`，阶段成功或失败后使用单调时钟记录包含程序与 Validator/QC 在内的完整耗时秒数，并保留三位小数；不恢复 `started_at` 等其他时间字段。

## v1.3.0（2026-07-23）

- 简化运行时 Manifest：所有阶段删除不参与执行分派的 `script`、`validator` 字段；orchestrator 按 `stage_id` 固定选择 handler 和验收函数，旧模板中的同名字段在创建新 run 时会被主动剥离。
- AI 兜底收敛到 `stage_1_standardize`。阶段二及以后由确定性程序执行，失败直接进入 `ERROR`，不再创建 fallback 请求或记录 AI 兜底字段。
- 阶段一继续固定执行 `validate_standardize()`；阶段二和阶段三保留必要的程序内守恒断言；阶段四固定执行一次 `validate_final()` 作为最终交付门禁。
- 新增固定的 `stage_1_results.json`：以文件内容 MD5 记录逐文件 `PENDING / DONE / BLOCKED / ERROR`、标准化产物和最小 route，替代 `stage_1_routes.json` 与 Manifest 的 `route_artifact`。
- 新增独立 `qc_results.json` 和四字段 QC 规则注册表，区分文件级、客户级规则以及检查点；规则异常不采用 fail-fast，HARD 阻断、SOFT 记录并继续确定性阶段。
- 支持显式 `parent_run_id` 的 Stage 1 单文件复用：仅复用直接父 Run 中同 MD5、同名、同 Skill 版本且产物存在的 `DONE` 文件，失败、新增或不满足条件的文件重跑；Stage 2～4及 QC 每个 Run 重新执行。
- 阶段一改为逐文件捕获错误并原子保存结果，一个文件失败不丢弃其他成功产物；AI fallback 请求只列出 `BLOCKED/ERROR` 文件。
- 最终交付物的“封面与说明”和“人工复核事项”读取当前 Run 的 QC 摘要，展示 QC 状态以及未通过的 HARD/SOFT 规则；规则结果仍以独立 `qc_results.json` 为事实源。
- 同步 Skill 协议、Manifest 回执与测试，明确 Manifest 只记录阶段状态和阶段一兜底信息。

## v1.2.20（2026-07-18）

- 统一发布版本事实：根目录 `pyproject.toml` 与 `assets/manifest.template.json` 同步更新为 `1.2.20`，运行 Manifest、分发包命名和已安装 Skill 使用同一版本号。
- 重新打包并安装当前项目 Skill，同步当前 `SKILL.md`、orchestrator、Manifest 模板与内置共享 core，消除本机已安装旧版与项目源码的漂移。
- 以程序产物为基准最小化修正 AI 兜底 Prompt：Prompt 2/3 的标准流水字段清单补充 `开户行`、`账户类型`；Prompt 2 仅替换 `排序规则` 的数组内容，改为按账户选择余额断点最少的候选行序。不改 Prompt 1 文件画像口径，不改 Prompt 2/3 的 JSON 字段结构。

## v1.2.19（2026-07-17）

- PDF 单元格换行统一改为无分隔符拼接，不再由 YAML 为不同字段选择 `space`、`cjk_join` 或 `remove_all`；同一视觉行内原有空格继续保留，换行造成的名称和账号断裂则直接消除。
- 坐标 Reader 按视觉行保留行内词间空格、跨视觉行无空格拼接；跨页续行同样不再人为插入空格。日期与时间粘连继续由标准化核心 `parse_datetime` 兼容解析。
- 删除 PDF YAML 的 35 项 `column_transforms` 及对应加载分支，更新受影响的 4 个 fingerprint ID。回归中曾小园招行原有 29 笔空对手账户全部恢复，曹吉安建行跨行账号可正常提取。

---

## v1.2.18（2026-07-16）

- 新增工商银行个人 OpenPDF 简版流水指纹：从抬头提取户名和卡号，过滤旋转水印字符；与含对方户名、对方账号的详版声明为同一系列。江西赣驰简版 PDF 完整解析 2,500 笔，交易时间、金额和余额均无缺失。
- Excel Reader 新增 YAML 选项 `reader_options.multi_sheet_same_layout`：仅合并表头完全一致的非空 Sheet，并将来源行号记录为 `Sheet名!行号`。江西赣驰 2025、2026 两份多 Sheet XLS 分别由 17、387 笔恢复为 936、718 笔。
- 阶段二新增“同一明确账户跨文件精确重叠”去重：要求交易时间、收支方向、金额和交易后余额完全一致，且各来源唯一对应；保留标准字段更完整的记录，同源歧义不删除。两份夏侯军刚工行 PDF 折叠重叠 124 笔，全部保留含对方户名和账号的详版记录。
- 将 `rural_account_detail_query_biff_v1` 系列的四个“账户明细查询结果”指纹统一标记为 `江西农商银行`；不同创建人、字体粗细变体保持同一 Router 银行口径，账户类型仍由后续证据判断。
- 工行对公 iText 时间戳模板补充稳定标题、Producer、本方账号户名和账号提取，明确识别为中国工商银行对公流水；伪后缀 `.pdf_2` 文件进入显式跳过报告，提示改名及扫描件 OCR 要求。
- 定向重跑江西赣驰：4 个有效文件原始 5,418 笔，跨文件去重 124 笔，交付 3 个账户、5,294 笔；工行账户余额校验通过，2025、2026 企业流水开户行统一为江西农商银行。
- 阶段三统一交易关系处理入口：保留支付宝商家订单号取消配对，并新增银行文件内相邻冲正识别。银行冲正必须同时满足来源行相邻、日期和本方账户一致、金额方向相反、对手一致、冲正后余额恢复以及附言兼容；成功配对的原交易与冲正行互相关联并将分析金额归零，未形成闭环的冲正只进入人工复核。定向重跑蒋华华识别 5 组、待复核 0 组。
- 农行“账户明细查询”模板补充抬头户名与掩码账号提取；日期与时间拆列时保留逐笔原始精度，时间为空输出日期而非伪造 `00:00:00`，整合阶段从交易值逐笔判断时间精度，文件级画像仅作审计汇总。

---

## v1.2.17（2026-07-15）

- Reader YAML 新增 `reader_options.source_order`，支持 `ascending` / `descending`。已配置时 Stage 1 按模板声明保持或翻转原始行序；未配置时继续使用余额连续性在原序、整体翻转、日期候选与余额链之间自动判断。
- 修正工商银行个人“明细查询文件下载”XLSX fingerprint：只使用与账户余额同一记账币种口径的 `记账金额(收入/支出)`，不再把 `交易金额` 占位列重复映射为标准金额；同时声明原始导出顺序为 `descending`。
- 定向回归广州沛瑾家具：个人账户 1,474 笔，收入合计 2,276,959.33 元、支出合计 2,302,721.48 元，余额断点由 1,329 降为 0；客户交付物 2 个账户、2,171 笔均通过余额校验，并已覆盖 `testoutput/20260714152213`。
- 新增回归测试，分别覆盖 YAML 行序优先和未配置行序时自动余额排序；Reader/路由相关 38 项测试通过。

---

## v1.2.16（2026-07-14）

- 农行“账户明细”XLSX fingerprint 增加抬头提取，从同一行的 `账号/户名/币种` 中恢复本方账户和本方名称；丰城市利华2025年5月644笔不再形成独立未识别账户。
- 民生银行个人账户对账单 fingerprint 增加抬头提取：`客户姓名` 写入本方名称，`客户账号` 写入本方账户；避免把同一抬头中的内部“账户账号”误作当前流水卡号。其组合列“对方户名/账号”通过 YAML `extract_mapping` 拆为对手名称和对手账户，不再让支付宝等账号残留在名称中。
- 阶段二新增同账户分卷归并：同 fingerprint、银行、账户类型和文件名家族的相邻文件，只有在明确身份不冲突且边界余额精确到分连续时才建立稳定 `source_series_id`。文件名只召回候选，不能单独触发归并；整合报告保留成员文件、边界金额、余额差额和证据交易编号。
- 将 JasperReports/iText 导出的“交易明细”对公 PDF 模板明确识别为上饶银行；标题样式收窄为 `STSong-Light`、24pt、非粗体、页面顶部居中且中心偏差不超过 3%，降低通用标题误命中风险。
- 更新 fingerprint ID 与 Router 回归断言；范新春 `交易明细111/B/A` 三份分卷继续命中同一模板。

## v1.2.15（2026-07-14）

- 通用 `extract_mapping` 提前到金额方向解析前执行，允许 YAML 将原始“其他”等值映射为标准收支方向；Python 核心不硬编码微信业务词。
- 微信 PDF/Excel fingerprint 配置补充 `零钱充值`、`零钱提现`、`转入零钱通`、`零钱通转出` 的方向映射；标签权威源 Excel 增加“零钱充值/零钱提现”，并重新生成 `assets/tag_rules.csv`，统一归入“内部调拨类 / 自有资金调拨 / 类现金余额产品调拨”。
- 定向回归邓子威微信流水：1,515 笔正常解析，其中原先金额为空的 30 笔全部恢复（提现 19 笔、充值 8 笔、转入零钱通 3 笔）；客户全目录仍为 2,771 笔，已重跑覆盖 `testoutput/20260714152213`。

## v1.2.14（2026-07-14）

- 修复戴金旺两份 PDF 的抬头身份提取：微信支付证明兼容“如下”跨行，提取微信号 `wxid_q7atqd3k4x2822`；江西农商银行流水从同一抬头行精确拆分户名与账号，避免本方名称被污染为“戴金旺 账”。
- 定向回归两份真实样本共 3,867 笔，本方名称统一为“戴金旺”，本方账户分别为微信号与农商银行账号，交付物中占位账户降为 0；已重跑覆盖 `testoutput/20260714152213`。

## v1.2.13（2026-07-14）

- 修复曾小园招商银行 PDF `对手信息` 双数字段拆分：保留原有单数字段兜底，并在存在首尾两个数字段时取最后一个作为 `对手账户`、中间文本作为 `对手名称`。目标样本 337 笔中，原文确无账号的 29 笔继续留空，另 29 笔名称中的账号残留由 29 降为 0。
- 定向回归曾小园招商银行 PDF，并重跑覆盖 `testoutput/20260714152213` 中的曾小园交付物。

## v1.2.12（2026-07-13）

- 将 `run_manifest.json` 的运行上下文合并进 `runs/<run-id>/manifest.json`，新运行不再生成双 manifest；顶层统一记录 `client`、`parent_run_id`、`rerun_reason`，阶段内继续记录脚本、校验器、AI 兜底引用、状态和兜底产物。
- 精简阶段运行字段：删除 `ai_fallback_dir`、`started_at`、`duration_seconds`；兜底目录固定为 `fallback/<stage-id>/`，实际失败原因保留在该目录的 `fallback_request.json`，不在 manifest 重复存储。
- 客户名称与归档名称统一：命名入口只保留 `--client`，显式参数优先，否则使用原始输入文件夹名；删除正式入口 `--force-name` / `--infer-client-name`、底层 `--customer` / `--force-customer`、上游 `client_alias` 运行记录及核心客户名补写/覆盖分支；批量入口同步改为可重复的 `--client`。`--client` 只负责客户与归档命名，不进入 `本方名称` 标准化逻辑。
- AI 兜底重跑必须显式传 `parent_run_id`；子运行从父 manifest 继承客户名称、失败阶段和兜底产物，禁止按目录“最新运行”猜测父运行。旧 `run_manifest.json` 仅保留父运行读取兼容，不再写入。
- 回归测试：24 个 manifest、客户名称、打包和 AI 兜底契约测试通过；斑马商业对公流水程序路线 5 阶段全部 `DONE`，仅生成统一 manifest 与 `斑马商业对公流水_已清洗_待分析.xlsx`。
- AI 兜底回放：父运行 `20260713T161835+0800-0f430ace` 在阶段一记录受控失败、`fallback_request.json` 和 `ai_repair.json`；子运行 `20260713T161840+0800-3fa893c3` 继承客户名与父运行 ID，重新执行 5 阶段并通过最终验收。
- 分发打包器补充排除 `testoutput/`、`原始流水数据/` 和独立 `tools/`，避免把运行产物、客户原始流水或 benchmark 工具写入 Skill zip。
- 删除无独立事实来源的 `主体名称`：最终流水不再生成该列，封面删除“整合主体数”，“主体账户清单”收敛为按 `本方账户` 分组的“账户清单”并展示文件证据中的 `本方名称`；Prompt 5、validator 和整合报告字段同步更新。
- `router_bank`、`inferred_bank`、`batch_pair`、`bank_source` 收敛为内部银行识别证据：阶段一文件画像和整合报告继续保留判断链路，整合过程继续使用 `__*` 临时列；整合流水、打标流水和最终 Excel 不再输出这 4 个字段。

## parser-iteration-2026-07-09-v1

- 问题归类：`reader-convergence`。开泰银行 PDF `泰国开泰银行/111.pdf` 原先由专用 `kasikorn_pdf_text.py` + `pdf_fixed_width` 接管；实际 PDF 有稳定真实竖线，更适合独立收敛为 `pdfplumber_grid_line_table`。
- 正式修改：新增 `pdfplumber_grid_line_table`，语义为“真实竖线确定 x 列边界，`row_anchor` 确定 y 行边界”；开泰 YAML 不写列坐标，只保留 `row_transforms`、方向派生、金额/余额拆分、金额列清洗和日期/时间列正则清洗；删除已不再引用的 `kasikorn_pdf_text.py`。
- 回归对比：以上一提交旧版专用 reader 产物为基准，重跑 `泰国开泰银行/111.pdf` stage1。新旧均为 484 行；收入 94 笔、支出 390 笔；收入合计 6,605,153.89，支出合计 6,626,488.12 完全一致。剩余差异集中在少量 `交易渠道` / `账户方附言` 的竖线列归属，新版按 PDF 真实竖线归位。
- 问题归类：`reader-convergence`。农行文本 PDF `26060214275857136186.pdf` 原先由专用 `abc_text_pdf.py` 接管；实际 PDF 视觉层为稳定 word 坐标列，适合收敛到通用 `pdfplumber_word_column_table`。
- 问题归类：`reader-convergence`。浙江庆元农商 PDF `GRZD-9A202606081958362818-20250608-20260607-X_unsign_sign_18831.pdf` 原先由专用 `zhejiang_qyrcb_pdf_text.py` 接管；实际 PDF 不能可靠读出二维表格，也不是完整文本行，但表头 x 坐标稳定、交易日期可作为行锚点，适合收敛到新通用 `pdfplumber_coordinate_table`。
- 正式修改：新增 `reader_options.row_transforms` / `reader_options.column_transforms` 读取配置，`pdfplumber_coordinate_table` 复用 word 坐标落列能力，并支持 `continuation: until_next_anchor`，按“当前交易日期到下一个交易日期前”确定交易块 y 范围；浙江庆元规则改为 `reader_id: pdfplumber_coordinate_table`，删除已不再引用的 `zhejiang_qyrcb_pdf_text.py`。
- 字段口径修正：浙江网商银行 PDF/Excel fingerprint 将 `企业名称 -> 本方名称`、`企业账号 -> 本方账户` 明确写入 `fingerprint.columns.all` 派生映射；标准化核心补充抬头标签 `企业名称`，并在内容/文件名均无法识别开户行时使用 router 的银行上下文作为低优先级开户行来源。
- 回归对比：以 `testoutput/20260709094408/_package_work/048_程旭/_工作区/程旭/*网商*__pdf__standardized.csv` 为旧产物，重跑 3 份网商 PDF 生成 `testoutput/mybank_enterprise_name_20260710/`。新旧行数分别保持 1088、1402、2371，收入/支出汇总完全一致；旧版 `本方名称=对方账号`、`开户行=中国银行/平安银行/招商银行` 修正为 `本方名称=企业名称`、`开户行=浙江网商银行`，交易唯一编号随本方名称修正发生预期变化。
- Reader 命名收敛：YAML、Python 分发和测试统一使用 `pdfplumber_coordinate_table`，旧 reader_id 兼容入口已删除；内部坐标函数统一使用 `_coordinate_*` 命名。`pdfplumber_coordinate_table` 先走 word 坐标表格化，若抽出列数少于字符串分隔表格结果，则回退采用 separator 结果。
- 数据质量改善：括号续行、中文断行按视觉列归属。例如旧产物将 `（大小额）` 放入 `银行备注`，新版按 x 坐标归入 `交易渠道=人行接口（大小额）`；`存款日终记账API` 的 x 坐标位于 `备注` 列，新版归入 `银行备注`，不再误作 `交易渠道`。
- 回归对比：以 `testoutput/20260706145056/_package_work/025_李先根/_工作区/李先根/GRZD-9A202606081958362818-20250608-20260607-X_unsign_sign_18831__standardized.csv` 为旧产物，重跑 stage1 生成 `testoutput/zhejiang_coordinate_table_20260709/GRZD-9A202606081958362818-20250608-20260607-X_unsign_sign_18831__standardized.csv`。新旧均为 240 行，本方名称/本方账户、fingerprint_id、银行、decision 一致；CSV 差异集中在 `银行备注` 与 `交易渠道` 的续行归属和中文断行合并。
- 正式修改：农行 fingerprint 的 `reader_id` 从 `pdf_fixed_width` 调整为 `pdfplumber_word_column_table`；`fingerprint.columns.all` 直接声明复合源字段 `日 志 号`；通用 word-column reader 支持多 word 表头匹配、`row_anchor.pattern` 和 `column_transforms`。
- 字段口径：按 PDF 视觉列归位 `交易渠道` / `交易附言`。例如 `大额支付` 进入 `交易渠道`、`网商银行转账` 进入 `账户方附言`；`短信费`、`个人活期结息` 位于右侧附言列时进入 `账户方附言`。
- 数据质量改善：`对手信息` 使用 `newline: cjk_join`，将 `国网江西省电力有` + `限公司`、`丰城市农村人居环` + `境投资有限公司` 等断行公司名拼回对手名称，避免误落入附言。
- 清理历史残留：删除已收敛的 `abc_text_pdf.py`；同步清理已不再引用的支付证明专用 reader 残留，微信/支付宝继续通过通用 reader + transform 路径处理。
- 回归对比：以 `testoutput/20260709094408/.../26060214275857136186__standardized.csv` 为旧产物，重跑 stage1 标准化生成 `testoutput/abc_word_column_compare_20260709/new_current/26060214275857136186__standardized.csv`。新旧均为 395 行 × 17 列，金额、余额、交易时间无变化；差异集中在 `对手名称`、`账户方附言`、`交易渠道`，以及因对手名称修正导致的 `交易唯一编号` 变化。
- 流程要求：以后凡是 reader / fingerprint / stage1 标准化字段口径相关改动，必须至少重跑目标文件 stage1，比较新旧 `*__standardized.csv` 和 `*__mapping.json`；多数 reader/fingerprint 改动不需要跑整合、打标、package，除非改动影响去重、标签、余额或交付物阶段。
- 问题归类：`reader-convergence`。江西裕民银行 PDF `APPLY2026060214573700135618149968_trade_history_sign.pdf` 原先由专用 `jiangxi_yumin_bank_pdf.py` 接管；实际 PDF 为稳定 word 坐标列表，可收敛到 `pdfplumber_word_column_table`。
- 正式修改：江西裕民 fingerprint 的 `reader_id` 从 `pdf_fixed_width` 调整为 `pdfplumber_word_column_table`；`fingerprint.columns.all` 直接声明 `业务摘要 -> 收支方向`、`交易金额(元) -> 交易金额`、`交易类别 -> 银行备注`、`对方户名/对方账号 -> 对手名称/对手账户`；`转入/转出` 由标准化核心的方向判断统一识别，fingerprint 不再通过 `column_transforms.values` 做值语义替换。中文断行字段仍使用 `newline: cjk_join` 做单元格清洗。
- 数据质量改善：新 reader 按视觉列把续行的 `（非支票）`、`银联全渠道贷记发卡侧入账` 等保留在 `银行备注`，避免旧专用 reader 把备注续行拼入 `对手名称`。
- 回归对比：以 `testoutput/yumin_word_column_compare_20260709/old_special_reader/APPLY2026060214573700135618149968_trade_history_sign__standardized.csv` 为旧产物，重跑 stage1 生成 `testoutput/yumin_word_column_compare_20260709/new_word_column/APPLY2026060214573700135618149968_trade_history_sign__standardized.csv`。新旧均为 289 行 × 17 列；交易时间、本方名称、本方账户、开户行、账户类型、对手账户、来源文件名、来源行号一致；收入金额、支出金额、交易金额、账户余额逐格一致。差异为：289 行 `银行备注` 去除了旧 reader 拼入的方向词 `转入/转出`，20 行 `对手名称` 修正备注续行污染，并由此导致对应 `交易唯一编号` 变化。
- 文档归口：删除本地 `references/router-设计.md`，Router 层次设计统一维护在 Obsidian「流水分析工程」中，避免 repo 内重复设计文档和知识库事实源分叉。
- 问题归类：`reader-convergence`。江西农商银行 PDF `艾晓林/江西·农商银行(2026年05月20日11时29分50秒)-2.pdf` 原先由专用 `jxrcb_pdf_text.py` 接管；实际 PDF 为稳定 word 坐标列，水印是可按字体/字号过滤的字符层噪声，适合收敛到通用 `pdfplumber_word_column_table`。
- 正式修改：江西农商 fingerprint 的 `reader_id` 调整为 `pdfplumber_word_column_table`；新增通用 `word_filters.drop_chars` 支持按字符、字体、字号过滤水印，新增 `word_filters.stop_line_contains_any` 支持在页尾汇总行前停止取词；word-column reader 同时从表头上方 key/value words 生成 `preamble`，供现有账户嗅探继续识别 `户名`、`账号`。
- 数据质量改善：`交易摘要`、`对方户名` 使用 `newline: cjk_join`，`对方账号` 使用 `newline: remove_all`。例如旧 reader 将 `跨行转出-南昌巨鲸农牧发展有限公司` 读成 `西跨行转出-南昌巨鲸农牧西南昌巨鲸农牧发展有限西` 且 `对手名称` 为空；新版读为 `银行备注=跨行转出-南昌巨鲸农牧发展有限公司`、`对手名称=南昌巨鲸农牧发展有限公司`、`对手账户=36050182035200000593`。
- 回归对比：以 `testoutput/jxrcb_word_column_compare_20260709/old_special_reader/江西·农商银行(2026年05月20日11时29分50秒)-2__standardized.csv` 为旧产物，重跑 stage1 生成 `testoutput/jxrcb_word_column_compare_20260709/new_word_column/江西·农商银行(2026年05月20日11时29分50秒)-2__standardized.csv`。新旧均为 1328 行 × 17 列；收入金额、支出金额、交易金额、账户余额汇总完全一致，本方名称/本方账户逐格一致；差异集中在 `银行备注` 330 行、`对手名称` 230 行、`对手账户` 4 行，属于水印字符清理和换行公司名拼接修正，并导致 230 行 `交易唯一编号` 随内容指纹更新。

## parser-iteration-2026-06-25-v4

- 调用来源：WorkBuddy workspace `2026-06-24-19-10-09` 问题 4，余额校验出现冲正交易断点。
- 问题归类：`standardization-core-gap`。部分银行冲正行在原始金额列仍带负号，但余额实际按逆操作增加；标准化核心应把此类 `冲正` 负金额翻正为收入。
- 正式修改：`SYNONYMS["银行备注"]` 增加 `交易名称`，让中行样本的 `冲正` 可进入标准备注；`单列带符号` 金额模式下，`银行备注` 含 `冲正` 且交易金额为负时翻为正数。
- 边界：若专属 parser 或原始文本已将冲正金额解析为正数，不做二次反转。
- 回放测试：`test_workbuddy_parser_iteration_20260624.py` 覆盖中行与农行冲正行不产生余额断点；目标客户全流程回放生成交付物时 `余额预警账户 0`。

---

## parser-iteration-2026-06-25-v3

- 调用来源：WorkBuddy workspace `2026-06-24-19-10-09` 问题 3，微信支付证明 PDF 中 `收/支/其他=其他` 的 563 笔金额被清空。
- 问题归类：`parser-gap` / 微信专属模板语义解析缺失；不在通用金额逻辑中硬编码微信规则。
- 正式修改：补强 `pdf_rules.yaml` 的 `wechat_pay_proof_pdf` 完整表头结构指纹，新增 `parsers/wechat_pay_proof_pdf.py` 专属 parser，并由 `router.py` 按 YAML 命中结果分发。
- 金额方向：`零钱提现`、`经营账户提现`、`转入零钱通-来自零钱` 记为支出；`零钱充值`、`零钱通转出-到零钱` 记为收入。标签和剔除逻辑不在本阶段处理。
- 回放测试：`test_pdf_router_decision.py` 覆盖 YAML 路由命中；`test_workbuddy_parser_iteration_20260624.py` 覆盖微信五类 `其他` 交易金额方向。

---

## parser-iteration-2026-06-25-v2

- 调用来源：WorkBuddy workspace `2026-06-24-19-10-09` 问题 2.2，阶段一存在 `skipped_inputs` 时仍被判定成功，导致 AI 兜底未触发。
- 问题归类：`validator-gap`。业务成功标准调整为：所有候选 PDF/Excel 文件必须成功标准化；只要候选输入进入 `skipped_inputs`，阶段一 validator 即失败。
- 正式修改：`scripts/validate_stage.py::validate_standardize` 增加 `skipped_inputs` 拦截；`scripts/orchestrator.py` 在阶段一验证时传入运行时 `manifest.skipped_inputs`。
- 回放测试：新增 `tests/test_validate_stage.py`；临时混合输入回放确认 `bad.xlsx` 被 skip 时 run 状态为 `error`，并生成 `fallback/stage_1_standardize/fallback_request.json`。

---

## parser-iteration-2026-06-25-v1

- 调用来源：WorkBuddy workspace `2026-06-24-19-10-09` 问题 2，两个可抽取文本 PDF 被阶段一跳过。
- 修复 `26060214275857136186.pdf`：农行 PDF 真实文本层表头为 `日 志 号`，将 `abc_text_pdf` 路由规则改为该稳定 marker，命中既有 parser。
- 修复 `APPLY2026060214573700135618149968_trade_history_sign.pdf`：新增 `jiangxi_yumin_bank_pdf` 文本 parser，并接入既有江西裕民银行 PDF 路由。
- 补充 `客户姓名` 抬头识别，确保裕民银行样本能回填本方名称。
- 回放测试：`bank-statement-standardization/tests/test_workbuddy_parser_iteration_20260624.py`；整包回放 run `/private/tmp/ymb-problem2-runs/20260625T105418+0800-39efd2a2` 成功，阶段一 `processed_files=5`，`skipped_inputs=[]`。

---

## v1.2.11（2026-06-24）

- 新增 `references/prompt-1a-输入读取与文件识别.md`，将阶段一 AI 兜底收敛为加密 PDF/Excel 无法打开时的密码补充流程：向用户确认密码，写入客户目录 `_file_hints.yaml`，再由阶段一入口程序重跑。
- `assets/manifest.template.json` 的 `stage_1_standardize.ai_fallback_refs` 增加 Prompt 1A，并新增轻量 `ai_fallback_info`，只说明该阶段 AI 兜底关注点，不把 Prompt 判断正文写入 manifest。
- `SKILL.md` 明确 manifest / run_manifest / receipts 的职责边界，并允许 `_file_hints.yaml` 作为客户输入提示文件由阶段一入口读取。
- `AGENTS.md` 补充就近原则、文档最小化原则，以及运行时输出契约归属规则，避免仓库级规范重复维护阶段字段细节。
- 阶段一字段映射提示词更名为 Prompt 1B，明确只处理已成功读取并抽取样本行的文件。

---

## v1.2.10（2026-06-23）

- 将共享标准化内核从 `packages/ymb_standardization_core/` 迁移为仓库平级子项目 `ymb-standardization-core/`；Skill zip 分发包内仍自包含到 `bank-statement-standardization/packages/ymb_standardization_core/`。
- 将阶段事实源模板从 skill 根目录 `manifest.json` 迁移到 `assets/manifest.template.json`，根目录不再放资源模板；运行时仍复制为 `runs/<run-id>/manifest.json`。
- 将项目运行约束统一升级到 Python 3.11，并以 `testdata/陈荣武` 完成正式流水标准化全流程验证。

---

## v1.2.9（2026-06-16）

- `scripts/orchestrator.py` 新增确定性 zip 输入快照能力：`--folder` 可直接传单个 `.zip`，preflight 前会解包到本次独立 `runs/<run-id>/input`，后续阶段仍只读取该快照目录。
- zip 解包支持常见中文文件名编码修复：优先按 `cp437->gbk`、`cp437->gb18030` 解码，失败时保留原始文件名；目录项只创建目录，不再被误当作文件写入。
- 当 zip 内所有文件共享单一顶层父目录时自动剥离该父目录，避免出现 `张运贞/张运贞/...` 这类嵌套输入；多顶层目录则保留原目录结构，避免同名文件冲突。
- 增加 zip 安全边界：拒绝绝对路径、盘符路径和 `..` 路径，所有解包输出必须位于本次 `run/input` 内；同一输出路径冲突时中止，不静默覆盖。
- `run_manifest.json` 和 `receipts/01-preflight.json` 新增 `input_snapshot` 审计信息，记录原始 zip、zip SHA256、解包目录、编码策略、剥离的公共父目录、目录项、解包文件清单、跳过项和冲突项。
- 新增回归测试覆盖 zip 中文文件名、单一父目录剥离、路径穿越拒绝和 preflight receipt 解包清单，避免后续再由 AI 临时创建解包脚本。

---

## v1.2.7（2026-06-15）

- 优化 `scripts/tag.py` 的规则匹配性能：按收支方向预构建候选规则与规则组，并对同组关键词编译字面量正则粗筛；最终仍按原规则优先级和顺序逐条确认，保持标签判定口径不变。
- 新增 `direction_series` 批量判定收支方向，替代逐行重复数值解析，降低大批量流水打标阶段的运行开销。
- 为交易打标优化新增回归测试，覆盖批量方向判定、同条件规则顺序保持、交错规则组不被错误重排等场景，避免性能优化改变命中结果。
- 优化 `scripts/package_deliverable.py` 的交付物工作簿样式处理：在 `ExcelWriter` 持有的 workbook 上直接设置冻结窗格、表头样式、列宽和金额格式，避免写出后再二次读取整份 xlsx。
- 新增交付物工作簿回归测试，明确禁止样式阶段重新 `load_workbook` 读回已保存文件，减少大交付物 stage_4 的额外 I/O。
- 正式编排器 `scripts/orchestrator.py` 启动时会把 `--folder` 输入目录复制到本次运行工作区 `input/` 下，后续阶段统一从该快照读取；运行 manifest 同时记录原始输入目录和快照目录，便于审计与重跑。

---

## v1.2.6（2026-06-12）

- 删除 `SKILL.md` 中重复维护的 `最小字段口径`/`最小接口契约` 小节，避免主协议、validator 和附件说明形成多事实源。
- 删除 `SKILL.md` 中独立的 `阶段索引` 和 `AI 兜底` 小节，将正常推进、失败兜底、AI 动作约束和重跑协议统一并入 `状态机`。
- 合并 `SKILL.md` 中空置的 `正式入口` 与 `工作空间` 标题，减少无内容层级。
- `references/附件A-标准化字段说明.md` 补充打标后字段：`收支方向`、`标签来源`、`标签置信度`、`命中规则编号`、`命中关键词`。
- `references/附件B-标签体系参考.md` 新增标签输出字段说明，明确标签来源、置信度、命中规则和命中关键词的追溯口径。
- 统一依赖事实源：源码仓库以根目录 `pyproject.toml` 的 `standardization` 可选依赖组管理标准化运行依赖；Skill 分发包中的 `requirements.txt` 仅作为部署兼容安装清单，并删除容易误导为完整锁定文件的 `requirements-lock.txt`。

---

## v1.2.5（2026-06-11）

- Token Vault 批量包不再向下游传递明文 `archive_name`；`orchestrator.py` 会忽略旧 manifest 中残留的 `archive_name`，避免 tokenized 流程反向泄露真实客户名。
- 上游如需归档展示名，只允许传递非敏 `client_alias`，并可传递 `archive_id` 作为审计关联标识；未提供可用别名且当前名称是 `tokenized_batch_bundle`、`tokenized_*`、`*_batch_bundle` 等技术名时，运行时写入 `CLIENT_NAME_UNCONFIRMED` warning。
- `run_manifest.json` 的 `upstream_manifest` 仅记录 `archive_id`、`client_alias` 与 `archive_name_present` 布尔值，不记录明文 `archive_name`。
- `package_skill.py` 与源码快照排除 `build/`、`*.egg-info/`，避免共享 core 的构建产物污染 Skill 分发包和运行审计快照。
- 新增回归测试覆盖 Token Vault manifest 明文归档名忽略、非敏别名接收、技术目录名告警和构建产物排除。

---

## v1.2.4（2026-06-11）

- 将单文件标准化内核从 Skill 内部实现迁移为仓库级共享包 `ymb-standardization-core`，作为标准化 Skill、脱敏项目等上层流程共同依赖的标准化事实源。
- `bank-statement-standardization/scripts/standardize.py` 保留为兼容旧命令的薄入口，直接定位并调用 `ymb_standardization_core`；旧的 `scripts/standardization/` 目录已移除，避免代理文件造成双事实源误解。
- `scripts/package_skill.py` 打包时自动把共享 core 包写入 Skill zip 内的 `ymb-standardization-core/`，确保 WorkBuddy 单独安装标准化 Skill 后仍可运行。
- 更新 `README.md`，明确源码态共享 core 位于仓库根目录 `packages/`，打包态随标准化 Skill 一起进入 zip。
- 新增/更新测试覆盖共享 core 包导入、旧 CLI 兼容、打包包含 core 包，避免标准化 Skill 与脱敏项目后续依赖路径漂移。

---

## v1.2.3（2026-06-10）

- 拆分 PDF 银行 parser 路由：新增 `standardization/parsers/router.py`，将江西农商文本 PDF、农行文本 PDF 等解析逻辑从 `standardize.py` 中剥离，避免主标准化脚本继续膨胀。
- 新增江西农商 PDF 路由测试，验证路由层能识别目标 PDF 并命中对应 parser，降低后续同类文件依赖 AI 兜底的概率。
- 抽取标准化主逻辑为 `scripts/standardization/core.py`，`scripts/standardize.py` 收敛为薄入口，便于后续 parser、OCR、字段映射和 validator 分层迭代。
- 新增 `test_standardization_package.py`，验证标准化包入口可导入、可复用，避免重构后 CLI 与包调用路径漂移。
- 新增 `workbuddy-parser-iteration/SKILL.md`（流水标准化迭代器草稿），用于从 WorkBuddy workspace 范围巡检 AI 兜底、分类 parser 缺口，并按一次调用形成一个标准化 Skill 迭代版本。

---

## v1.2.2（2026-06-09）

- 修复指定 workspace / 指定工作空间场景下的幻觉风险：`SKILL.md` 明确禁止 AI 用描述、截图、目录名或猜测信息替代真实 workspace、run、manifest 和 receipts。
- 强化重跑审计：`orchestrator.py` 补充 `parent_run_id`、`rerun_reason`、父 run 继承信息和运行级审计记录，便于串联首次失败、AI 兜底和后续重跑。
- 改进账户元数据与客户名推断：`standardize.py` 保留并输出更完整的本方账户、户名、开户行等报告元数据，降低归档名和主体识别误判。
- 新增 `references/prompt-5-交付物组装.md`，将交付物组装阶段的 AI 兜底资料显式归档到 references。
- 新增回归测试：`test_client_name_inference.py`、`test_orchestrator_manifest.py`、`test_standardize_report_metadata.py`，覆盖客户名推断、编排器 manifest 审计和标准化报告元数据。
- 调整打包协议说明：`CORE_PROTOCOL.md` 与 `SKILL.md` 补充交付物打包与审计约束，要求错误包、告警包、运行产物继续保持可追溯。

---

## v1.2.1（2026-06-08）

- `SKILL.md` 收敛为唯一 harness 协议入口，只保留正式入口、状态机、AI 兜底、写入边界、红线和最小索引。
- 移除独立 `CORE_PROTOCOL.md`，避免协议在多个文件中重复和漂移。
- `manifest.json` 使用 `ai_fallback_refs` 直接指向对应阶段的 prompt/reference；AI 只在阶段失败后读取当前阶段兜底资料，orchestrator 只记录不执行。
- `manifest.json` 新增 `ai_fallback_used`，运行时阶段一旦触发 AI 兜底即写为 `true`。
- `manifest.json` 使用 `started_at` 记录阶段开始时间，使用 `duration_seconds` 记录阶段验收通过时的耗时秒数。
- `run_id`、`run_manifest.json`、运行时 `manifest.json`、`events.jsonl`、`receipts/*.json` 的时间统一使用北京时间 `+08:00`。
- `manifest.json` 新增 `ai_fallback_dir` 和 `ai_fallback_artifacts`；阶段失败时自动生成 `fallback_request.json`，AI 兜底开发的脚本、补丁、参数文件必须保存在运行目录并可追溯。
- `receipts/*.json` 的阶段脚本回执新增 `script`、`orchestrator_handler`，validator 回执新增 `validator`，用于记录实际由哪个脚本/入口执行。
- `orchestrator.py` 只执行运行时 `manifest.json` 状态机，不读取 `SKILL.md`；`SKILL.md` 是 AI/harness 的入口协议。
- 删除 `references/业务知识总览.md`，将独有边界归位到阶段 prompt，并新增 `prompt-5-交付物组装.md` 作为 `stage_4_package` 兜底资料；prompt 编号不等同于状态机阶段编号。

---

## v1.1.19（2026-06-03）

- 修复 AI 把文件夹名/猜测名当作确认客户名传入 `--client` 的问题：`--client` 未配套 `--client-confirmed` 时仅作为临时归档名，仍会优先按流水证据推断真实归档名。
- 新增 `--client-confirmed`，只有用户明确确认归档名时才允许固定 `--client`；`--force-name` 必须同时传 `--client-confirmed`，避免用猜测名覆盖本方名称。
- `CORE_PROTOCOL.md`、`SKILL.md`、`README.md` 增加硬约束：AI 不得根据文件夹名、截图或描述构造确认客户名。

## v1.1.18（2026-06-03）

- `orchestrator.py` 改为真实按运行时 `manifest.json` 状态机逐阶段执行：复制 skill 根目录模板到 run 目录，扫描第一个 `status != DONE` 的阶段，执行脚本、执行 validator，通过后写 `DONE`。
- 每个阶段开始前写入 `CORE_PROTOCOL_LOADED` 和 `STAGE_START` 事件，阶段完成写入 `STAGE_DONE`；审计信息改写到 `run_manifest.json`，避免污染阶段状态机。
- 阶段失败时写入 `AI_FALLBACK_REQUIRED`，按当前阶段 `ai_fallback` 进入有限兜底入口，并把阶段状态写为 `ERROR` 后中止打包。
- 客户名推断改为基于 `本方账户 + 账户余额` 的银行账户证据，按账户聚合并按账户数、余额行覆盖、来源文件数评分；支付流水商户/脱敏格式不再参与归档名投票。
- 用 `testdata/2` 完成逐阶段状态机回归测试，运行时 manifest 五个阶段均为 `DONE`，最终交付物按当次归档名生成。

## v1.1.17（2026-06-03）

- 新增 `CORE_PROTOCOL.md`，明确 WorkBuddy 执行核心协议：脚本优先、AI 有限兜底、阶段检测、可恢复、可验收、可追溯。
- 新增 `manifest.json` 阶段事实源模板，统一主流程阶段 ID：`stage_1_standardize → stage_2_integrate → stage_2b_portfolio_balance → stage_3_tag → stage_4_package`。
- 统一状态机关键词为英文 ID，中文仅作为 `name` 或标题展示；`ai_fallback` 统一指向 `SKILL.md#stage_*`。
- 更新 `SKILL.md` 和 `README.md`，要求真实任务先加载 `CORE_PROTOCOL.md`，再读取运行时 `manifest.json`，按 `script / validator / ai_fallback / status` 执行。
- 用 `testdata/2` 完成正式入口回归测试，生成交付物并通过最终检测；客户名多主体识别产生 WARNING 归档，未阻断交付。

## v1.1.2（2026-06-02）

- 新增正式生产主入口 `scripts/orchestrator.py` 和阶段验收 `scripts/validate_stage.py`。
- 告警任务继续执行，并在完成后生成 `*__WARNING__full.zip`；错误任务立即中断并生成
  `*__ERROR__full.zip`。两类归档均包含完整输入和已执行日志。
- 正式入口默认不限制模型，不依赖宿主支持 `/model` 命令或注入模型环境变量；仅在显式传入
  `--require-model` 时启用可选模型核验。
- 修复户名覆盖：`--customer` 改为缺失兜底，原始 PDF/Excel 已识别户名优先保留；新增
  `--force-customer` 供人工确认后强制覆盖。
- 微信支付交易明细证明保留首页证明文本，并识别 `兹证明：<姓名>` 格式。
- Windows 终端提示改用 `[WARNING]` / `[SKIP]`，避免 GBK 控制台输出特殊符号时报错。
- 新增 `requirements-lock.txt` 作为部署约束文件。正式任务直接使用宿主机 `python`，不检测或自动安装特定
  Python 版本、不创建私有 venv、不套 PowerShell、不在任务运行期间安装依赖；缺少依赖时在部署阶段
  执行一次约束安装。
- `requirements-lock.txt` 从 Python 3.8 专用完整 freeze 改为主依赖兼容范围，避免默认 Python 3.11+ /
  3.13 安装 `pandas==2.0.3`、`numpy==1.24.4` 等旧版本冲突。
- 正式主入口允许省略 `--client`，优先使用主流程内部识别出的唯一户名归档；无法唯一识别时才使用输入
  文件夹名。禁止 AI 为猜测户名预读 PDF 或逐份调用 `standardize.py`。

## v1.1（2026-06-02）

相对 **v1.0（初始版）** 的全部改动，按主题归类。涉及脚本主要在 `scripts/`，口径文档在 `references/` 与 `SKILL.md`。

### 一、字段口径变化（影响所有下游）

- **新增列 `开户行`**：由文件名 / 抬头 / 表头中的银行名推断为规范行名（工农中建交、招行/浦发/中信…
  及三湘/长沙/上饶/江西/邮储/农商/农信等）；无法确认留空并进人工复核。
- **新增列 `账户类型`**：对公 / 个人 / 未知，随行数据贯穿标准化→整合→打标→底表/交付物
  （v1.0 只把账户类型放在报告里、丢在行数据外，混合对公对私批量时无法区分）。
- **`交易金额` 改为统一回填**：`交易金额 = 收入金额(空当0) − 支出金额(空当0)`，带符号（流入正、流出负）；
  收支皆空才留空。v1.0 仅在「单列带符号 / 方向+金额」结构下填，分列结构留空、且方向+金额时符号可能为正。
- **`交易唯一编号` 改为内容指纹**：`md5(本方名称/账户 + 交易时间 + 对手名称/账户 + 收入 + 支出 + 余额)`，
  不再含来源文件/行号；同一笔交易跨文件得到相同编号，作为去重依据；同文件内真实重复笔加 `-2/-3` 后缀。
- **标准化明细列序**：`交易唯一编号 交易时间 本方名称 本方账户 开户行 账户类型 对手名称 对手账户
  收入金额 支出金额 交易金额 账户余额 银行备注 账户方附言 交易渠道 来源文件名 来源行号`。
- **交付物主表列序**：`银行备注 / 账户方附言` 移到 **`虚拟账户余额` 之后**（金额→余额→备注/附言→标签→渠道/来源）。

> 兼容性：旧版 `*__standardized.csv`（无开户行/账户类型列）仍可被 `integrate.py` 读取，缺列自动补空。

### 二、多文件拼接去重（修复重复流水）

- `integrate.py` 新增 **跨文件去重** `dedup_cross_file`：交易唯一编号（内容指纹）完全一致即视为同一笔交易的
  跨文件再导入（PDF 与 Excel 同源、整份文件重复提交），**折叠为一笔**，保留优先级：结构化表格 > PDF、
  字段更全、来源行号更小；移除明细写入报告 `跨文件去重`，并在概览给出 `原始交易数 / 跨文件去重笔数`。
- 仅折叠「完全一致」者；部分字段一致的疑似重复仍**只标记、不删除**（红线不变）。

### 三、识别更多表头格式 + 交易时间必填

- 同义词词典新增：`入账日期→交易日期`、`入账时间→交易时间`、`对方单位→对手名称`、
  `转入金额→收入金额`、`转出金额→支出金额`、`本方账号→本方账户`（修复 `转入/转出金额` 误命中「交易金额」）。
- **拆列日期+时间**（如 `入账日期`+`入账时间`）自动合并为完整时间戳；移除过宽的裸 `日期`/`时间` 词
  （避免把 `起息日期`/`到期日期`/`打印时间` 误当交易时间）。
- 修复 **紧凑时间戳** `YYYYMMDDHHMMSS[毫秒/流水号]`（如农行「交易时间戳」`20230201195010370199`）的解析
  （v1.0 会把日期段误当时分秒、精度被毁）。
- **交易时间作为必填字段**：列缺失、或部分行解析为空，都升级为人工复核项。

### 四、行序按余额连续性还原（消除伪余额断点）★ 核心修复

- v1.0 在余额校验/输出时**按交易时间排序**，导致：① 同秒/同日多笔被打乱；② 倒序对账单被搅乱；
  产生大量伪余额断点。
- 改为**以余额为对账真值**：`standardize.best_continuity_order` 在「原序 / 整体翻转 / 按日期升序·日内原序 /
  按日期升序·日内翻转 / 余额链重建」中选**余额断点最少**的行序。
  - **余额链重建**：每行紧跟在「余额 = 本行余额 − 本行净额」的那一行之后，专治同秒多笔与「内部记账序≠时间戳」。
- 两层应用：标准化按文件整理；`integrate.order_accounts_for_continuity` 按账户对**跨文件合并、去重后**的行重排。
- 下游 `portfolio_balance.py` / `multi_customer.py` **保留整合后的行序**（用行序 `__seq`），不再按时间重排。
- 新增 **汇总/页脚行过滤**：交易时间无有效日期且无对手/摘要/附言（如农行「总收入笔数/总支出金额」及其数值行）丢弃。
- 实测（丰城利华 80 文件 / 32,384 笔）：文件内余额断点 **由数百降至 0**，仅余跨月/跨文件边界的真实缺口。

### 五、自动排除杂入的非流水文件

- 新增筛选层（`standardize.classify_ext / screen_files / NotABankStatement`）：
  - 图片 / 扫描件 / Word / PPT / 压缩包等格式 → 跳过并说明原因；
  - **图片型/扫描件 PDF**（抽不到文本）→ 跳过，提示先 OCR；
  - **非流水表格**（如混入的发票、名册：缺「金额/余额」或缺「时间/余额」定位列）→ 跳过；
  - 无关文件（.DS_Store 等）静默忽略；流水线自身产物（`*__打标流水.csv` 等）不被当原始数据重复摄入。
- 被跳过的文件**全部留痕**：`run_pipeline` 打印清单；交付物封面有「已跳过文件数」、「人工复核事项」表逐条列出。

### 六、交付物可复用已有产物（`--reuse`）

- `package_deliverable.py` 新增 `--reuse 产物目录或 *__打标流水.csv/*__整合流水.csv`：直接用 `run_pipeline`
  已有的整合/打标产物组装交付物，**跳过重复跑标准化/整合/打标**；与 `--folder`/`--subject` 共用同一尾段
  （`_finalize`），交付物口径完全一致。避免「为省算力而手工拼表」导致列序/虚拟余额/去重等保证丢失。

### 七、余额断点诊断（识别「解析不全」≠「排序问题」）

- 余额校验新增 `断点率` 与 `疑似解析不全` 字段：断点率 > 30% 时直接判为「极可能解析不全/漏行/只读了文件
  部分行（大表被截断或抽样），请核对原始文件总行数并用脚本完整重跑」，而非「对账单排序问题」。
- `SKILL.md` 增加强约束：**大文件必须用脚本解析，切勿让模型逐行读原始表再转写**（上下文截断会只读到一小部分行，
  断点率畸高即此症状）。

---

## parser-iteration-2026-07-14-v1（招商银行同名 PDF/XLSX）

- 修复阶段一同名不同格式产物覆盖：统一保留扩展名后缀，分别输出
  `__pdf__standardized.csv` 与 `__xlsx__standardized.csv`，两份成功产物均进入阶段二。
- 修复招商银行个人流水 XLSX 混合布局：首页合并单元格交易与后续网格交易统一解析；从抬头提取
  本方名称/账户；按逐笔余额恒等式修复少量丢失两位小数的金额与余额。
- 修复招商银行个人流水 PDF 分页表头、页码、尾页验真说明污染，以及跨行对手名称错挂问题。
- 阶段一验证器拦截误混入交易行的中英文日期表头。
- 陈鑫伟正式回放 run `20260714T135559+0800-336ded76`：PDF 835 笔、XLSX 835 笔均成功；
  阶段二跨文件去重 835 笔，最终 1171 笔，两个账户余额断点均为 0。
- 定向回归吕建光、付亮亮、宁聚、斑马商业、曾小园、曾耀夏伟鹏、金伟共 7 份招行 PDF，
  合计 6,482 笔；交易时间、本方名称、本方账户及页脚污染检查均为 0 异常。

---

## v1.0（初始版）

四阶段流程（标准化 / 单客户整合 / 交易打标 / 多客户底表）+ 组合余额 + 单文件交付物 + 可移植提示词包；
覆盖三种金额结构、日期时间拆列合并、分页噪声清洗、PDF 多行单元格、按账户余额连续性校验。
## v1.2.8（2026-06-15）

- WorkBuddy parser iteration：巡检 `C:\Users\28307\WorkBuddy\2026-06-15-15-31-01`，目标 run
  `20260615T154053+0800-fcd8e6f4`，父 run `20260615T153153+0800-2213c083`。
- 问题归类为 `parser-gap`：泰国开泰银行（Kasikorn Bank）英文文本版 PDF 有文本层但无稳定表格边框，
  通用 `pdfplumber.extract_tables()` 路径无法识别流水表头，父 run 阶段一触发 AI 兜底。
- 新增 `ymb-standardization-core/ymb_standardization_core/parsers/kasikorn_pdf_text.py`：
  按 `DD-MM-YY HH:MM` 文本行解析交易，识别 `K PLUS` / `K BIZ` / `ATM` / `CDM` 等渠道，
  通过交易类型推断收入/支出，并以带符号交易金额交给标准化层。
- 更新 PDF router：当文本同时包含 `K PLUS`、`K BIZ`、`AccountMR.`、`Account Number` 且存在足量开泰交易行时，
  命中 `kasikorn_pdf_text`，不再误走通用表格或未匹配文本路径。
- 增强抬头嗅探：带明确标签的英文户名支持空格姓名；带标签账号支持连字符账号，避免回退误取参考号。
- 新增回放测试 `tests/test_kasikorn_pdf_route.py`，使用 `testdata/泰国开泰银行/111.pdf` 验证：
  parser 命中 `kasikorn_pdf_text`，户名 `HUAHUA JIANG`，账号 `061-8-92723-7`，标准化 484 行，
  收入 94 笔、支出 390 笔。
- 修复归档名推断：允许有本方账户和账户余额证据支撑的英文个人姓名进入归档候选，避免 `HUAHUA JIANG`
  被纯英文过滤规则误排除，导致封面和交付物文件名退回输入文件夹名。
- 验证命令：
  - `python bank-statement-standardization\tests\test_kasikorn_pdf_route.py`
  - `python bank-statement-standardization\tests\test_client_name_inference.py`
  - `python bank-statement-standardization\tests\test_jxrcb_pdf_route.py`
  - `python -m unittest discover -s bank-statement-standardization\tests -p "test_*.py"`
  - `python bank-statement-standardization\scripts\orchestrator.py run --folder "D:\PYTHON_WORK\ymb-skill\bank-statement-standardization\testdata\泰国开泰银行" --run-root "D:\PYTHON_WORK\ymb-skill\runs"`，回放 run `20260615T162308+0800-4eafbb13` 生成
    `HUAHUA JIANG_已清洗_待分析.xlsx`。

---
